package updates;

/**
 * Created by Caroline on 02/04/2015.
 */
public class Lobby {
    // TODO json must store for every player, id, name and public key
    String json;

    public Lobby(String json) {
        this.json = json;
    }
}
